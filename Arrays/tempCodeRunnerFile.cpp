ize = sortedArray.insert(sortedNums, size, capacity, 11);
    // sortedArray.printArray(sortedNums, size);

    // size = sortedArray.insert(sortedNums, size, capacity, 1);
    // sortedArray.printArray(sorted